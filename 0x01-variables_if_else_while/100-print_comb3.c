#include <stdio.h>

/**
 * main - two digits
 *
 * Return: 0
 */
int main(void)
{
	int c = '0';
	int d = '0';

	while (c <= '9')
	{
		d = '0';
		while (d <= '9')
		{
			putchar(f);
			putchar(s);
			if (s == '9' && f == '9')
			{
			}
			else
			{
				putchar(',');
				putchar(' ');
			}
			s++;
		}
		f++;
	}
	putchar('\n');
	return (0);
}
